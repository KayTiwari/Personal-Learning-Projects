/**
 * @format
 */

import {AppRegistry} from 'react-native';
import MainApp from './src/MainApp';
import {name as appName} from './app.json';

AppRegistry.registerComponent('toggle', () => MainApp);
